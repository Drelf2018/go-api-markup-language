type query: "查询" = {
    num mid: 目标用户mid
    bool photo: 是否请求用户主页头图 = false
}

type card: "卡片" = {
    mid
    name
    str face
}

type ls<T> = [
    num
    num
    str
    bool: 猜
    T
]

type tls = [2]card